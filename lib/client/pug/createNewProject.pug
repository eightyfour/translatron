.createNewProjectView.overlayWindow
  h2 Create a new project
  // <label class="octicon octicon-puzzle">Which name has the category</label>
  .inputWrap.octicon.octicon-file-code
    input.blue.inputField.category(canny-mod='createNewProject', canny-var='createNewProjectInputProject', placeholder='enter the project name')
    .inputWrap-row
      button.blue.button(canny-mod='createNewProject', canny-var='createNewProjectSubmit') Create project
      button.red.button(canny-mod='translationView', canny-var='cancel', data-view='createNewProjectView') cancel

  p
    | A project name could be a translation task number. Or ask your developer
    | for a technical specific name.

